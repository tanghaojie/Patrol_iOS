# Uncomment the next line to define a global platform for your project
 platform :ios, '8.0'
use_frameworks!

target 'MyFramework' do
	pod 'SwiftyJSON'
        pod 'MJRefresh'
        pod 'SnapKit', '~> 3.2.0'
end
